<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/viewPagerBackgroundColor</item>
        <item name="tabSelectedTextColor">@color/colorText</item>
        <item name="tabIndicatorColor">#27ae60</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
    </style>
    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="textAllCaps">true</item>
    </style>
    <style name="ThemeName" parent="@style/AppTheme">
        <item name="actionMenuTextColor">@color/colorText</item>
        <item name="android:actionMenuTextColor">@color/colorText</item>
    </style>

</resources>
